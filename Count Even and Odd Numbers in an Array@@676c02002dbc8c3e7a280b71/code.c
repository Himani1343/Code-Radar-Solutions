#include <stdio.h>
int main(){

int arr[n];
    scanf("%d",&n);
    int countEven=0;
    int countOdd=0;
    for(i=0;i=n<=n-1;i++){
        if(arr[i]%2==0)
        countEven=countEven+arr[i];

        else
        countOdd=countOdd+arr[i];
    }
    return 0;
}