#include<stdio.h>
int main(){
    char x;
    scanf("%c",&x);
    if(n>=a && n<=z){
        printf("Lowercase");
    }
    else if(n>=A && n<=Z){
        printf("Uppercase");
    }
    return 0;
}